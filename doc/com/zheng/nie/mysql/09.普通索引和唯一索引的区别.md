#####查询过程
    1.普通索引在查找到满足条件的第一个记录之后，需要继续查找下一个记录。
    对于唯一索引来说，由于索引定义了唯一性，在查找待满足条件的记录之后，就会停止继续检索。innodb是以页为单位来进行读写的，
    在读取一条记录的时候，是将整页读取到内存之中，当找到第一个记录的时候，对于普通索引来说，需要再进行一次查找和判断吓一条记录。
#####更新过程
    当更新一个数据页的时候，如果数据页不在内存之中，innodb会将这些更新操作缓存在change buffer之中，这样则不需要从磁盘中读入数据页了，
    当下次查询需要访问这个数据页的时候，
